---
description: Copy this file into .cursor/rules/ (or global rules) to auto-run auto-skill-bootstrap
alwaysApply: true
---

# Auto Skill Bootstrap (always)

Use the deterministic skill bootstrap before doing deep work:

1) Rebuild inventory:

```bash
python3 .cursor/skills/auto-skill-bootstrap/bin/update-manifest.py
```

2) Pick 3–8 capabilities using:
- `.cursor/skills/find-skills/domains.json`
- `.cursor/skills/auto-skill-bootstrap/capabilities.json`

3) Search missing capabilities (no auto-install by default):

```bash
python3 .cursor/skills/auto-skill-bootstrap/bin/auto-skill-bootstrap.py --no-install \
  --cap <cap1> --cap <cap2>
```

If `candidates.json` has multiple good options, ask the user to choose. Auto-install only if explicitly allowed and only from allowlisted sources.

