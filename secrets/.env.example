## Local secrets (do NOT commit). Used by scripts/prepare-secrets.sh|ps1

# Claude Code custom endpoint auth token (used by cc-custom-helper)
CC_API_KEY=

# GitHub PAT (optional; for gh auth bootstrap, if you add it later)
GITHUB_TOKEN=
